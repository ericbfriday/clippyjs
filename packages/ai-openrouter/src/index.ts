/**
 * @clippyjs/ai-openrouter - OpenRouter provider for ClippyJS
 *
 * Implements AI provider interface for OpenRouter's unified API.
 * Supports multiple models from various providers with streaming, tools, and vision.
 */

export { OpenRouterProvider } from './OpenRouterProvider';
