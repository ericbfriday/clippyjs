{
  "name": "clippyjs-lib",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/clippyjs-lib",
  "projectType": "application",
  "tags": [
    "type:app",
    "scope:demo"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{projectRoot}/dist"
      ],
      "options": {
        "command": "echo 'Build configured in package.json'"
      }
    }
  }
}
