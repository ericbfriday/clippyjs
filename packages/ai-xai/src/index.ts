/**
 * @clippyjs/ai-xai - xAI (Grok) provider for ClippyJS
 *
 * Implements AI provider interface for xAI's Chat Completion API.
 * Supports Grok models with streaming and tools via OpenAI-compatible API.
 */

export { XAIProvider } from './XAIProvider';
