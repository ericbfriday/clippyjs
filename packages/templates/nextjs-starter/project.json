{
  "name": "clippyjs-nextjs-starter",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/templates/nextjs-starter",
  "projectType": "application",
  "tags": ["type:template", "scope:nextjs-starter"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "echo 'Template build configured in package.json'"
      }
    }
  }
}
